package com.miciaha.arraysAndStrings.implementations;
    /*
        An array that resizes itself when necessary. Creates an array of any type.

        Provides O(1) insertion time when array doesn't need to be expanded.
        Provides O(N) as worst case insertion
        The resizing factor in Java is 2
     */
public class CustomArrayList {
}
