package com.miciaha.arraysAndStrings.implementations;
    /*
        Similar to an array list but explicitly for strings.
        Holds an array of strings and copies them to a single string only when necessary.
     */
public class CustomStringBuilder {
}
